{% extends 'base.html.twig' %}

{% block title %} {{ user.username }}
{% endblock %}

{% block body %}
    <div class="container">
        <div class="row">
            <div class="card border-primary col-4">
                <h4 class="card-header text-center">Photo de profil</h4>
                <div class="card-body">
                    <p class="card-text">
                        {% if user.image is null %}
                        <img class="avatar_profil" src="{{ asset('Image/avatar.png') }}"
                             alt="avatar d'utilisateur non défini">
                        {% else %}
                            <img class="avatar_profil" src="{{ user.image.url }}" alt="Photo de l'utilisateur {{ user.username }}">
                    {% endif %}
                    image !
                    </p>
                </div>
            </div>
            <div class="card border-primary offset-1 col-7">
                <h4 class="card-header">Nom d'utilisateur : {{ user.username }}</h4>
                <div class="card-body">
                    <h4 class="card-title">Mon profil</h4>
                    <p class="card-text">Mon email : {{ user.email }}</p>
                    <ul class="row">
                        <li class="col-4">Mes derniers commentaires</li>
                        <li class="col-4">Mes derniers commentaires</li>
                        <li class="col-4">Mes derniers commentaires</li>
                    </ul>
                    {% if app.user == user %}
                        <a class="btn btn-primary" href="{{ path('app_update_user',{'user_id' : user.id}) }}">Modifier
                            mon profil</a>
                    {% endif %}
                </div>
            </div>
        </div>
    </div>

{% endblock %}
